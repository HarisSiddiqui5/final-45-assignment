"use strict";
//stroring the famous quote and its author
let quote = "Nothing is imposable because impossible itself I M possible";
let author = "Haris siddiqui";
//printimg the message
console.log(`"${quote}" -${author}`);
